meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: true
run:
  image: null
  command: null
  args: null
  persistenceMount: /data
  containerPort: "8080"
  servicePort: "8080"
